---
"@scrymore/scry-deployer": patch
---

Bump `@scrymore/scry-sbcov` to ^0.4.0: screenshot framing options (`captureMode: root`, `captureViewport`, `captureScale`) read from the project's `scry-sbcov.config.js`, so component stories are captured at the size of the Figma component they are compared against and phone-sized screens are no longer clipped at 720 px. No deployer flags change.
